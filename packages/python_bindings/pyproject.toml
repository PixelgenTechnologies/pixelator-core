[project]
name = "pixelgen-pixelator-core"
dynamic = ["version"]
requires-python = ">=3.10"
description = "Python bindings for performance critical parts of pixelator"
readme = "README.md"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8,<9"]

[tool.maturin]
module-name = "pixelator_core"

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"
