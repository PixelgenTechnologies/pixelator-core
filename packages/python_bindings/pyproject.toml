[project]
name = "pixelator_core_py"
version = "0.1.0"
description = "Python bindings for pixelator-core."
readme = "README.md"
dependencies = []

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"
